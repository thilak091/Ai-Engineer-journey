Day - 18
plt.pie ( values , labels = [ , ... ] , colors = [ , ... ] ,
wedgeprops = { 'edgecolor' : ' ' } )
Note:-
good to use when there is less than 5 columns.
to emphasis slices of the pie, use explode
argument and pass a list
explode = [ 0, 0, 0.1, 0, 0 ]
L-> This element will pop out
shadow = True
to add shadows
to get percentage automatically
autopct = '%1.1f%%' -> add this
Video - 2
Stack Plot
It is used to find the distribution of
something or multiple things over something
(like points scored by players over time).
plt. ( x_values, y_dist_1, y_dist_2, ... , labels = [ , ... ] ,
colors = [ , ... ] )
if labels needed to be shown, plt.legend()
Video 3
Filling Area on Line Plots
plt.fill_between( x_axis, y_axis_of_line_plot,
till_where, where = (condition),
interpolate = True, alpha = (0 to 1) )
y_axis_of_line_plot = under which lines value
we need to fill
till_where = till where in yaxis needed
to be coloured
where = (condition) => paints only if
condition is satisfied
alpha = 0.25 => Recomended , it will
be transparent
color = ' ' => custom color.
label = ' ' => will be in
legend.
